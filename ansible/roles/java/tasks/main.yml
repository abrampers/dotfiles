---
- name: java | darwin entrypoint
  ansible.builtin.import_tasks: darwin.yml
  when: ansible_facts['os_family'] == 'Darwin'

- name: java | unsupported platform placeholder
  ansible.builtin.debug:
    msg: "Java role currently supports Darwin only."
  when: ansible_facts['os_family'] != 'Darwin'
