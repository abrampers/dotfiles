---
- name: claude | darwin entrypoint
  ansible.builtin.import_tasks: darwin.yml
  when: ansible_facts['os_family'] == 'Darwin'

- name: claude | unsupported platform placeholder
  ansible.builtin.debug:
    msg: "Claude role currently supports Darwin only."
  when: ansible_facts['os_family'] != 'Darwin'
