---
- name: emacs | darwin entrypoint
  ansible.builtin.import_tasks: darwin.yml
  when: ansible_facts['os_family'] == 'Darwin'

- name: emacs | unsupported platform placeholder
  ansible.builtin.debug:
    msg: "Emacs role currently supports Darwin only."
  when: ansible_facts['os_family'] != 'Darwin'
