---
- name: cursor | darwin entrypoint
  ansible.builtin.import_tasks: darwin.yml
  when: ansible_facts['os_family'] == 'Darwin'

- name: cursor | unsupported platform placeholder
  ansible.builtin.debug:
    msg: "Cursor role currently supports Darwin only."
  when: ansible_facts['os_family'] != 'Darwin'
