---
- name: python | darwin entrypoint
  ansible.builtin.import_tasks: darwin.yml
  when: ansible_facts['os_family'] == 'Darwin'

- name: python | unsupported platform placeholder
  ansible.builtin.debug:
    msg: "Python role currently supports Darwin only."
  when: ansible_facts['os_family'] != 'Darwin'
