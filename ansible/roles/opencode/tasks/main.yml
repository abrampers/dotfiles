---
- name: opencode | darwin entrypoint
  ansible.builtin.import_tasks: darwin.yml
  when: ansible_facts['os_family'] == 'Darwin'

- name: opencode | unsupported platform placeholder
  ansible.builtin.debug:
    msg: "OpenCode role currently supports Darwin only."
  when: ansible_facts['os_family'] != 'Darwin'
