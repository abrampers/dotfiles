---
- name: ruby | darwin entrypoint
  ansible.builtin.import_tasks: darwin.yml
  when: ansible_facts['os_family'] == 'Darwin'

- name: ruby | unsupported platform placeholder
  ansible.builtin.debug:
    msg: "Ruby role currently supports Darwin only."
  when: ansible_facts['os_family'] != 'Darwin'
