---
- name: kubernetes | darwin entrypoint
  ansible.builtin.import_tasks: darwin.yml
  when: ansible_facts['os_family'] == 'Darwin'

- name: kubernetes | unsupported platform placeholder
  ansible.builtin.debug:
    msg: "Kubernetes role currently supports Darwin only."
  when: ansible_facts['os_family'] != 'Darwin'
