---
- name: git | darwin entrypoint
  ansible.builtin.import_tasks: darwin.yml
  when: ansible_facts['os_family'] == 'Darwin'

- name: git | unsupported platform placeholder
  ansible.builtin.debug:
    msg: "Go role currently supports Darwin only."
  when: ansible_facts['os_family'] != 'Darwin'
